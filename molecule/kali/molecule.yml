---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: bootstrap-kali
    image: kalilinux/kali-linux-docker
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: kali
