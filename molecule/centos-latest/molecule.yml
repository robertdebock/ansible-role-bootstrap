---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
lint:
  name: yamllint
driver:
  name: docker
platforms:
  - name: bootstrap-centos-latest
    image: centos:latest
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: centos-latest
